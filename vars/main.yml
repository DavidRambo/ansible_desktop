---
# vars file for ansible-role-gnome-extensions
# source: github.com/ctorgalson

gnome_extensions_url: "https://extensions.gnome.org"

gnome_packages:
  - name: "gnome-shell"
    state: present
  - name: "gnome-shell-extensions"
    state: present

gnome_extensions:
  - id: 3952 # workspace indicator
